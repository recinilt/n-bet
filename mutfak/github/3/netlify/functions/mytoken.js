/*
exports.handler = async (event, context) => {
    const token = process.env.MYTOKEN;
    return {
      statusCode: 200,
      body: JSON.stringify({ token }),
    };
  };
*/  
const token = process.env.MYTOKEN;
